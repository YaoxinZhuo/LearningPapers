function [Eps]=epsilon(x,k)

% Function: [Eps]=epsilon(x,k)
% ??
% Aim: 
% Analytical way of estimating neighborhood radius for DBSCAN ????????????????DBSCAN?????
% Input: 
% x - data matrix (m,n); m-objects, n-variables???? x ???? m?object n???
% k - number of objects in a neighborhood of an object??k???object????objects??
% (minimal number of objects considered as a cluster)??k??????????

% ?

[m,n]=size(x);

Eps=((prod(max(x)-min(x))*k*gamma(.5*n+1))/(m*sqrt(pi.^n))).^(1/n);	%prod ?? gamma????